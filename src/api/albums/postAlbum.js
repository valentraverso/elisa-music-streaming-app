const postAlbum = (data, token) => {
    if(!data){
        return{
            status: false,
            msg: "There is no info in post."
        }
    }

    const formData = new formData();

    formData.append('albumTitle', )
}